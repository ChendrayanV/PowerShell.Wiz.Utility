mutation updateServiceAccount($serviceAccountId: ID!, $scopes: [String!]) {
  updateServiceAccount(
    input: { serviceAccountId: $serviceAccountId, patch: { scopes: $scopes } }
  ) {
    serviceAccount {
      id
      name
    }
  }
}
